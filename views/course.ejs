<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-6">
        <h1 class="text-3xl font-semibold text-center mb-6">Courses</h1>
        
        <!-- Form to Add a New Course -->
        <div class="mb-6">
            <h2 class="text-xl font-semibold mb-2">Add a New Course</h2>
            <form action="/courses" method="POST" class="space-y-4">
                <input type="text" name="course_name" placeholder="Course Name" class="w-full p-2 border border-gray-300 rounded" required />
                <input type="text" name="schedule" placeholder="Schedule" class="w-full p-2 border border-gray-300 rounded" required />
                <button type="submit" class="w-full bg-blue-500 text-white p-2 rounded hover:bg-blue-600">Add Course</button>
            </form>
        </div>

        <!-- Display Courses -->
        <div class="overflow-x-auto">
            <table class="min-w-full table-auto bg-white shadow-md rounded-lg border-collapse">
                <thead>
                    <tr class="bg-gray-200">
                        <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Course Name</th>
                        <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Schedule</th>
                        <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% courses.forEach(course => { %>
                        <tr>
                            <td class="px-6 py-3 border-b text-gray-700"><%= course.course_name %></td>
                            <td class="px-6 py-3 border-b text-gray-700"><%= course.schedule %></td>
                            <td class="px-6 py-3 border-b text-gray-700 space-x-2">
                                <!-- Update Course Button -->
                                <a href="/courses/update-course/<%= course.id %>" class="inline-block bg-yellow-500 text-white p-2 rounded hover:bg-yellow-600">Edit</a>
                                
                                <!-- Delete Course Button -->
                                <form action="/courses/delete-course/<%= course.id %>" method="POST" class="inline-block">
                                    <button type="submit" class="bg-red-500 text-white p-2 rounded hover:bg-red-600">Delete</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
