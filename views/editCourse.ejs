<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= course.course_name %> Course</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-6">
        <h1 class="text-3xl font-semibold text-center mb-6">Edit Course</h1>

        <form action="/courses/update-course/<%= course.id %>" method="POST" class="space-y-4">
            <input type="text" name="course_name" value="<%= course.course_name %>" class="w-full p-2 border border-gray-300 rounded" required />
            <input type="text" name="schedule" value="<%= course.schedule %>" class="w-full p-2 border border-gray-300 rounded" required />
            <button type="submit" class="w-full bg-blue-500 text-white p-2 rounded hover:bg-blue-600">Update Course</button>
        </form>
    </div>
</body>
</html>
